# Authoring Templates

Template authoring follows a similar process to its predecessor/alternative of legacy expansion rule authoring. 

To create a new sequence template (or to edit existing ones) navigate to the **Sequence Templates** page of Aerie. Here

// click new template 

// specify a language (STOL, SEQN, or TEXT. Here's what each does.) Model ID (for access to activity type set) and Parcel ID (for access to commands). Finally, activity type 

// edit it in the editor. we have autocomplete functionality, and you can specify something in handlebars, format whitespace, and access diagnostics. show example of bad SEQN 

// you can also use helpers. here are the 4 we have defined so far (add-time, subtract-time, flatten, formatAsDate)

// finally, save it. show good example 

You are now ready to expand using this template.

